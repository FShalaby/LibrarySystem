## Tests

After running tests inside the `RandoopTest` class, please reset the CSV files to
their original state before running any other tests. This is because `RandoopTest`
will modify the CSV files, sometimes incorrectly which can cause issues.